#@ load("@ytt:data", "data")
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: externaldns-install-default-sa
  namespace: #@ data.values.externaldns.namespace
  annotations:
    kapp.k14s.io/change-group: externaldns-install/rbac
    kapp.k14s.io/change-rule: "delete after deleting externaldns"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: externaldns-install-default-role
  annotations:
    kapp.k14s.io/change-group: externaldns-install/rbac
    kapp.k14s.io/change-rule: "delete after deleting externaldns"

rules:
- apiGroups: ["*"]
  resources: ["*"]
  verbs: ["*"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: externaldns-install-default-role-binding
  annotations:
    kapp.k14s.io/change-group: externaldns-install/rbac
    kapp.k14s.io/change-rule: "delete after deleting externaldns"
subjects:
- kind: ServiceAccount
  name: externaldns-install-default-sa
  namespace: #@ data.values.externaldns.namespace
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: externaldns-install-default-role
